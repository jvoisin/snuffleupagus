; Regression test for @set variable definition and redefinition.
; The second @set must replace the first, so `myfunc` resolves to "foo".
@set myfunc "notafunc";
@set myfunc "foo";
sp.disable_function.function(myfunc).drop();
